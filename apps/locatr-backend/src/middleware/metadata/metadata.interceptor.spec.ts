import { MetadataInterceptor } from './metadata.interceptor';

describe('MetadataInterceptor', () => {
  it('should be defined', () => {
    expect(new MetadataInterceptor()).toBeDefined();
  });
});
